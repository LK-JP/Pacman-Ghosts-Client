
// setup is run once on startup and is generally used to "set up" the canvas and any other neccessary initial functions
function setup() {
  createCanvas(200, 200)
}

// draw is run on a continuous loop at a max frames per second of 60, and is generally used to create visuals within the canvas
function draw() {
background(62)
}
